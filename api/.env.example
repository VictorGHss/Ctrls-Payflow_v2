# ===== CONTA AZUL =====
# Credenciais da aplicação (obter no Portal Conta Azul)
CONTA_AZUL_CLIENT_ID=seu_client_id
CONTA_AZUL_CLIENT_SECRET=seu_client_secret

# URL de callback (deve estar configurada no Portal Conta Azul)
# Para desenvolvimento local: http://localhost:8000/oauth/callback
# Para produção: https://seu-dominio.com/oauth/callback
CONTA_AZUL_REDIRECT_URI=https://seu-dominio.com/oauth/callback

# URLs da API Conta Azul v2 (NÃO ALTERAR a menos que a Conta Azul mude)
# Base URL para chamadas de API
CONTA_AZUL_API_BASE_URL=https://api-v2.contaazul.com

# Base URL para autenticação OAuth2
CONTA_AZUL_AUTH_BASE_URL=https://auth.contaazul.com

# Endpoints OAuth2 (derivados das bases acima, mas podem ser sobrescritos se necessário)
# Authorization: onde o usuário faz login - https://auth.contaazul.com/login
# Token: onde trocamos code por tokens - https://auth.contaazul.com/oauth2/token
# Scope padrão: openid profile aws.cognito.signin.user.admin

# ===== SEGURANÇA =====
MASTER_KEY=base64_encode_de_32_bytes_aleatorios
JWT_SECRET=seu_jwt_secret_aleatorio_aqui

# ===== SMTP =====
SMTP_HOST=smtp.seuhost.com
SMTP_PORT=587
SMTP_USER=seu_email@dominio.com
SMTP_PASSWORD=sua_senha_smtp
SMTP_FROM=seu_email@dominio.com
SMTP_REPLY_TO=seu_email@dominio.com
SMTP_USE_TLS=true
SMTP_TIMEOUT=10

# Override de destinatário (OPCIONAL - apenas para testes)
# Se setado, TODOS os emails serão enviados para este endereço
# em vez do email real do médico
# IMPORTANTE: Deixar vazio em produção!
# Exemplo para testes: OVERRIDE_RECIPIENT_EMAIL=testes@seudominio.com
# OVERRIDE_RECIPIENT_EMAIL=

# ===== POLLING =====
POLLING_INTERVAL_SECONDS=300
POLLING_INITIAL_LOOKBACK_DAYS=30
# Janela de segurança: voltar N minutos para não perder eventos
POLLING_SAFETY_WINDOW_MINUTES=10

# ===== API =====
API_HOST=0.0.0.0
API_PORT=8000
API_LOG_LEVEL=info

# ===== CLOUDFLARE TUNNEL =====
CLOUDFLARE_TUNNEL_TOKEN=seu_token_aqui

# ===== DATABASE =====
DATABASE_URL=sqlite:///./data/payflow.db

# ===== FALLBACK MEDICOS =====
# JSON ou CSV com mapping: nome_cliente -> email_medico
DOCTORS_FALLBACK_JSON={}

